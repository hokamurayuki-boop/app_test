# 夜勤手当計算アプリ 計画書

作成日: 2026-04-11
更新日: 2026-04-11 (アーキテクチャ変更: Python CLI → Webアプリ/Netlify)

---

## 0. アーキテクチャ方針（2026-04-11 更新）

**Python + openpyxl ローカル実行案 → Netlify 静的Webアプリ案** に変更。

| 項目 | 採用内容 |
|---|---|
| 公開方法 | Netlify (静的ホスティング) |
| 実装 | HTML + JavaScript (ES5/ES6) |
| Excel読み書き | [SheetJS (xlsx)](https://sheetjs.com/) を CDN から利用 |
| サーバー処理 | **なし**（勤怠データは全てブラウザ内で処理） |
| 運用 | ブラウザでURLにアクセス → ファイル選択 → 計算 → 結果ダウンロード |

**採用理由:**
- 勤怠データ（個人情報）がサーバーに送信されないため、情報セキュリティ観点で安全。
- Python実行環境の構築・配布が不要。担当者は URL を開くだけで使える。
- Netlify 無料枠で運用可能。デプロイは Git push で自動。
- 計算ロジックは当初の Python 案と同一（計画書 3章）なので、要件は全く変わらない。

**ファイル構成:**
```
アプリテスト/
├── index.html       # UI
├── app.js           # 計算ロジック + Excel I/O + テスト
├── style.css        # スタイル
├── netlify.toml     # Netlify 設定
└── 夜勤手当計算アプリ_計画書.md
```

---

## 1. 目的

エクセルで管理している勤怠データから、規定に基づく夜勤手当を自動計算し、結果をエクセルへ書き戻す（または別シートに出力する）アプリを作成する。

手計算・目視による集計の手間とミスをなくし、給与計算業務を効率化することを目的とする。

---

## 2. 対象業務（現状）

- 勤怠は Excel ブックで管理されている。
- 給与計算ソフトの仕様上、**同一シフトでも日付が変わるとエクセル行が分割される**。
- 夜勤手当は、勤怠担当者が目視で各行を確認しながら計算している（と想定）。

---

## 3. 計算ロジック（要件）

### 3.1 基本ルール
| 項目 | 内容 |
|---|---|
| 夜勤時間帯 | **22:00 〜 翌 08:00** |
| 単価 | **1,000 円 / 時間**（夜勤時間帯にかかった分のみ） |
| 1勤務あたりの上限 | **5,000 円** |

### 3.2 行をまたぐシフトの扱い（重要）

給与計算ソフトは日付をまたぐと行を分割するため、**1つのシフトが2行に分かれて記録される**。
アプリは以下の手順で処理する必要がある。

1. **シフトのグルーピング**
   - 同一従業員 × 連続する時間帯（前行の終了時刻 = 次行の開始時刻）を 1 シフトとしてまとめる。

2. **行ごとの夜勤時間算出**
   - 各行の勤務時間と夜勤時間帯（22:00〜翌8:00）の重なりを分単位で計算。

3. **シフト合計で上限判定**
   - シフト単位で合計夜勤手当（仮）を算出。
   - 5,000 円を超える場合は **5,000 円に丸める**。

4. **行への再配分**
   - 先頭行から順に割り当て、上限に達したら以降の行を切り詰める（FIFO 方式）。
   - 例示のケースと一致することを確認済み。

### 3.3 具体例（仕様書の例）

勤務: 4/10 20:00 〜 4/11 08:00

| 行 | 開始 | 終了 | 行内勤務時間 | 夜勤時間(22-8) | 素点手当 | 配分後手当 |
|---|---|---|---|---|---|---|
| 1 | 4/10 20:00 | 4/11 00:00 | 4h | 2h (22-24) | 2,000 | **2,000** |
| 2 | 4/11 00:00 | 4/11 08:00 | 8h | 8h (0-8) | 8,000 | **3,000** |
| 計 | | | 12h | 10h | 10,000 | **5,000** |

→ シフト合計 5,000 円上限。Row1 に 2,000 割当 → Row2 に残り 3,000 割当。

---

## 4. 確認したい事項（要ヒアリング）

計画確定のため、以下を確認させてください。

1. **エクセルの構造**
   - ブック / シート名・行列の構成（列名、ヘッダ行の位置）
   - 従業員を識別する列（社員番号？ 氏名？）
   - 日付・開始時刻・終了時刻の列の形式（日付と時刻が同じセル？ 別セル？）

2. **シフト境界の判定方法**
   - 同一シフト判定は「前行の終了 == 次行の開始」で厳密に連結するか？
   - それとも「同日 or 翌日0:00で分割」というシンプルな前提でよいか？

3. **出力方法**
   - 既存シートに「夜勤手当」列を追加して書き込みか
   - 別シート / 別ファイルに集計出力か
   - 従業員ごとの月次合計も必要か

4. **上限配分の方式**
   - 例の通り「先頭行優先（FIFO）」でよいか
   - あるいは「時間数按分」等の別方式があるか

5. **エクセルファイルの取り扱い**
   - ローカル PC 上のファイルを開いて処理するか
   - OneDrive / SharePoint 上か
   - 実行中に他ユーザーが開いている可能性はあるか

6. **既存スキル「night-shift-allowance」との関係**
   - 同名の社内スキル（訪問介護シフト CSV → Google Spreadsheet 転記）が存在するが、今回は **Excel ⇄ Excel** のため別実装になる想定。
   - 計算ロジックのみ流用可能か確認する。

---

## 5. システム構成（確定版）

```
┌────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ 勤怠 Excel     │→ │  ブラウザ (JS)    │→ │ 結果 Excel       │
│ (ユーザーPC)    │  │  - SheetJS 読込   │  │ (ブラウザ DL)    │
└────────────────┘  │  - 計算           │  └──────────────────┘
                    │  - SheetJS 書込   │
                    └──────────────────┘
                         ↑ HTMLのみ
                    ┌──────────────────┐
                    │ Netlify (静的配信) │
                    └──────────────────┘
```

### 5.1 技術選定（確定）

| 項目 | 採用 | 理由 |
|---|---|---|
| ランタイム | ブラウザ JavaScript | インストール不要、データ外部送信なし |
| Excel ライブラリ | SheetJS (xlsx@0.18.5) | CDN 配信、読み書き対応、デファクト |
| ホスティング | Netlify | Git 連携で自動デプロイ、無料枠で十分 |
| ビルドツール | なし | 静的 HTML/JS のみ、シンプル |

---

## 6. データモデル（内部表現）

```python
@dataclass
class AttendanceRow:
    row_index: int          # Excel 上の行番号
    employee_id: str
    start: datetime         # 開始日時
    end: datetime           # 終了日時
    raw_night_minutes: int  # 夜勤帯の重なり（計算後）
    raw_allowance: int      # 素点手当（計算後）
    final_allowance: int    # 上限配分後の手当（出力値）

@dataclass
class Shift:
    employee_id: str
    rows: list[AttendanceRow]  # 連続する行のリスト
    total_raw: int
    total_final: int           # min(total_raw, 5000)
```

---

## 7. 実装ステップ

1. **サンプルデータの取得**（実ファイル 1 週間分程度）
2. **読み込み処理**: 従業員・日時列をパースして `AttendanceRow` 化
3. **夜勤時間算出関数**: `(start, end) → 22-8 帯と重なる分数`（単体テスト必須）
4. **シフトグルーピング**: 従業員単位で時系列ソート→連結判定
5. **上限適用＋FIFO 再配分**
6. **Excel 書き戻し**（指定列へ数値を書き込み、書式を崩さない）
7. **テスト**: 仕様書の例・上限未満・上限超過・単独行・3行以上のシフト など
8. **運用**: 実行手順書の作成、担当者への引き渡し

---

## 8. テストケース（最低限）

| # | 内容 | 期待値 |
|---|---|---|
| T1 | 仕様書の例（20:00-翌8:00） | Row1=2000, Row2=3000 |
| T2 | 夜勤帯のみ 5h（22:00-03:00） | 5000（上限ちょうど） |
| T3 | 夜勤帯 3h（22:00-01:00） | 3000 |
| T4 | 夜勤帯ゼロ（09:00-17:00） | 0 |
| T5 | 3 行にまたがるシフト | 合計 5000、FIFO 配分 |
| T6 | 同一従業員の別日シフト（非連続） | 別シフトとして個別上限 |

---

## 9. リスク・懸念点

- **シフト連結の誤判定**: わずかな時刻ずれで別シフト扱いになる恐れ → 許容誤差（例: 0分厳密 or ±5分）をヒアリング。
- **Excel 書式破壊**: openpyxl は一部書式を保持できないケースあり → 実ファイルで事前検証。
- **同時編集**: 担当者が Excel を開いていると書き込めない → 実行前のクローズを運用ルールに。
- **休憩時間の扱い**: 夜勤帯内に休憩がある場合、控除するか不明 → 要確認。

---

## 10. 次のアクション

1. 上記「4. 確認したい事項」への回答をいただく。
2. サンプル Excel（匿名化可）を 1 ファイル共有いただく。
3. 合意後、プロトタイプを 1 ファイル構成（Python スクリプト）で作成し、仕様書の例で動作確認。
4. 実運用データで検証 → 本番投入。
